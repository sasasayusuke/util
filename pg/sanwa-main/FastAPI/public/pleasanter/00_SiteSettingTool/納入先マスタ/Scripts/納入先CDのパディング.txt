$('#Results_ClassD').on('change',function(){
    const deliveryCD = $('#Results_ClassD').val();
    if(/^\d+$/.test(deliveryCD)){
        const result = ("0000" + deliveryCD).slice(-4);

        $p.set($p.getControl('ClassD'),result).trigger('change');


    }
})