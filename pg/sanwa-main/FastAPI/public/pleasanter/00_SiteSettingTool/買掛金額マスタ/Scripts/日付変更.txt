$p.on('change', 'DateC', function () { //売掛日付
    AA()
})

//日付を1日に変更する関数
function AA(){

    let Date=$p.getControl("DateC").val()
    let parts = Date.split("/");
    parts[2] = 1;
    let newDate = parts.join("/");
    $p.set($p.getControl('DateC'), newDate)

}